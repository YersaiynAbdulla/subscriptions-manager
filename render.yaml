services:
  - type: web
    name: subscriptions-manager
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn subscription_manager.wsgi
